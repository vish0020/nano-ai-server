services:
  - type: web
    name: nano-ai-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn nano_transmitter_api:app --host 0.0.0.0 --port 10000
    plan: free
    
